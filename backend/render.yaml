services:
  - type: web
    name: student-gateway-backend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start:prod
    rootDir: backend
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
